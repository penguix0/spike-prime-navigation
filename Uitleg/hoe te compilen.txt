--== Dutch ==--
Er wordt vanuit gegaan dat python 3 en de dependencies al ooit geïnstalleerd zijn.

Installeer pyinstaller: pip install pyinstaller

Ga naar de directory met App.py

Voer het volgende command in:
pyinstaller App.py -i icon.ico

--== English ==--
This guide assumes that you already have python 3 installed aswell as the neccessary dependencies.

Instal pyinstaller: pip install pyinstaller

Go to the directory containing App.py

Execute the following command:
pyinstaller App.py -i icon.ico